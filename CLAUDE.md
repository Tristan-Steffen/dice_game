# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

**Fumble** — a Balatro-like 3D dice roguelike built in **Godot 4.7** (`gl_compatibility` renderer, Jolt physics). Code comments and in-game text are in **German**; identifiers are a mix. The whole game plays on a single table surface at world **Y = 0**, viewed as a "casino table screen": 2D UI is rendered into a `SubViewport` and displayed on a mesh (see Architecture). The table has **no edge**: the felt runs on as an endless floor that fades into the dark room (`TableGround`), leaving space for future features around the play area.

**Comments:** keep them to the necessary minimum — short German one-liners that state a non-obvious rule, constraint, or design decision. Don't restate what the code already says, don't duplicate a `description` string that sits one line below, and don't write essay-length narrative blocks or "siehe X" cross-reference chains. When in doubt, cut it.

## Commands

The Godot binary lives outside the repo at `E:/Godot/Godot_v4.7-stable_win64_console.exe` (use the `_console` build so stdout/stderr are captured).

**Erzeugte Bilder** (KI-Generierläufe, siehe Skill `asset-gen`) gehören nach `E:/Generated Images/<Projekt>/<Lauf>/` — nie ins Repo; nur der ausgewählte Gewinner wird nach `assets/` kopiert.

```bash
# Run the full test suite (headless, GUT). Exit code is non-zero on failure.
# --fixed-fps 60 koppelt die Frames von der Echtzeit ab (Delta fest 1/60), so
# laufen die wait_frames/wait_seconds der Integrationstests CPU-schnell statt in Echtzeit.
E:/Godot/Godot_v4.7-stable_win64_console.exe --headless --fixed-fps 60 --path . -s addons/gut/gut_cmdln.gd -gconfig=res://.gutconfig.json -gexit

# Run ONE test script while working (gemessen 3 s statt 150 s für die Suite; -gselect filtert Skripte nach Namensteil).
# -gtest fällt oft in die volle Suite durch, -gdir auf test/integration ist die halbe Suite - beides NICHT als Arbeitsschleife.
E:/Godot/Godot_v4.7-stable_win64_console.exe --headless --fixed-fps 60 --path . -s addons/gut/gut_cmdln.gd -gdir=res://test/integration -gselect=test_tower_view -gexit

# Register newly added `class_name`s in the class cache (EINMAL nach einem Umbenennen/Anlegen einer globalen Klasse - nicht bei jedem Lauf):
E:/Godot/Godot_v4.7-stable_win64_console.exe --headless --path . --import

# Run the game (headless boot check: --quit-after 200):
E:/Godot/Godot_v4.7-stable_win64_console.exe --path .

# One-off visual/logic probe script (ad-hoc scene), IMMER mit --quit-after (Frames; 600 = 10 s bei 60 fps) und off-screen:
E:/Godot/Godot_v4.7-stable_win64_console.exe --path . --resolution 1280x720 --position 4000,2400 --quit-after 600 --script <path-to.gd>
```

**Jeder Godot-Lauf endet von selbst, und keiner läuft im Hintergrund** (2026-09-05, nach einer 50-Minuten-Leiche): eine Probe, die VOR ihrem `quit()` auf einen Skriptfehler läuft, beendet sich nie — der Prozess lebt unsichtbar off-screen weiter. Darum trägt JEDE Probe und jeder Boot `--quit-after N` (gemessen: eine abgestürzte Probe endet damit nach 1–4 s statt nie), und jeder Godot-Start läuft im VORDERGRUND mit gesetztem Bash-`timeout` (Suite 600 s, Probe 120 s) — `run_in_background` hat keinen Timeout und ist für Godot verboten; `sleep`-Polling auf Logdateien ebenso. Test-Disziplin für Session UND Agenten: während der Arbeit nur EINZELNE Skripte per `-gselect`, die VOLLE Suite genau EINMAL am Ende (ein zweiter Vollpass nur nach einem echten Fehlschlag), keine `-gdir`-Teilsuiten, `--import` nur einmal nach einer Klassen-Umbenennung. Gemessen am 2026-09-05: sechs Agenten-Wellen zu 30–96 min, davon 3–10 Suiten-Läufe und 3–27 Sichtproben je Welle — die Testzeit allein war bis zu einem Drittel der Laufzeit.

CI (`.github/workflows/tests.yml`) runs the same GUT command on Linux after a `--editor --quit` import pass — as **two parallel matrix jobs** (`-gdir=res://test/unit` / `res://test/integration`) on separate runners, with `.godot` restored from `actions/cache` (the import pass dominates the job at ~9 min cold; warm it only re-imports what changed). Locally never split the suite into two concurrent Godot processes: one instance already saturates the machine (measured: parallel 135 s vs. serial 65 s). Test config is `.gutconfig.json` (scans `res://test/`, prefix `test_`, suffix `.gd`).

## Architecture

### Layered `scripts/` — the folder IS the dependency rule
`core/` and `data/` must not import from folders below them. New scripts are placed by layer, not by feature:

- **`data/`** — reine `Resource`-Records (inkl. `DealClause`); jede Id als `const`-String (single source of truth). Details: `scripts/data/CLAUDE.md`.
- **`core/`** — pure Logik, **keine Nodes** (`RefCounted`/static): Scoring, Effekt-Auflösung, `GameRun`, Verträge, Energie-Ökonomie. Details: `scripts/core/CLAUDE.md`.
- **`dice/`** — der physische Würfel: Physik, Builder, Face-Display, Schwebe-Stationen. Details: `scripts/dice/CLAUDE.md`.
- **`table/`** — 3D-Requisiten, Kamera und das Tisch-Display samt Bühnenmaschinerie (Vitrine, Hebebühne, Data-Cells, Licht-Routing). Details: `scripts/table/CLAUDE.md`.
- **`ui/`** — 2D-Fenster und Styling im Tisch-Display: Laden, Werkstatt/Presse, Magazin, Wetten. Details: `scripts/ui/CLAUDE.md`.

Die ausführliche Architektur-Doku liegt in diesen verzeichnis-lokalen CLAUDE.md-Dateien; Claude Code lädt sie, sobald dort Dateien angefasst werden. Wer ein Subsystem anfasst, liest die CLAUDE.md seines Ordners zuerst — Querverweise („siehe *X*") können in einer Schwesterdatei liegen.

### `scene_root.gd` — the single coordinator
Top-level `Node3D` (not in a subfolder). Owns exactly one `GameRun`, wires its signals to the UI, drives round flow (throw → score → take/reroll → round-goal → **Auszahlungs-Seite mit Kassieren-Halt** (der Hub wird zur Cash-Out-Seite und die Runde steht, bis „Kassieren" gedrückt ist — siehe *Die AUSZAHLUNGS-SEITE* in `scripts/ui/CLAUDE.md`) → shop — and the **pit is the shop's second exit**: focusing it while `phase == SHOP` means "Fertig", so `_on_camera_mode_changed` calls `ShopController.close()` and `_on_shop_closed` only zooms back to the overview when the camera really stands in the hub, never yanking back a flight already under way), and passes the same `GameRun` instance (typed) to `ShopController` and `WorkshopView`, which mutate state **only** through `GameRun` methods. It also does mouse-forwarding: `_forward_screen_mouse` projects window clicks onto the table plane and `push_input`s them into the `SubViewport` (the projection is camera-independent and runs in every pose, the free camera included); `_screen_forwards_pixel` asks which VISIBLE window owns the pixel — see *Sichtbar heißt bedienbar*. `_ready` is split into named setup steps (`_setup_dice`/`_setup_table_screen`/`_setup_camera_targets`/`_setup_panels`/`_setup_settings_ui`); camera raycasts go through the shared `_ray_pick(pos, mask)` helper and click zones through `_add_click_zone`.

### Globale Invarianten (Kurzform — Volltext in den Ordner-Dateien)
- **EINE Größe je Ding, überall** (Spieler-Entscheid 2026-09-04, Welle L): die Größe eines Objekts ändert sich NIE zwischen den Orten des Spiels, und ein kontext-eigener Maßstab ist ein Fehler, kein Feature. Eine Data-Card steht in `PackDrawerView.CASSETTE_SCALE` (2,184) — im Magazin, im Fach ihrer Treppenstufe, in der Laden-Vitrine, im Schwarzmarkt, als Wett-Gewinn, als geworfener Einsatz und auf der Auszahlungs-Ablage —, ein Würfel in `DiceTrayView.DIE_SCALE` (0,6). **Der ROLL folgt der LAGE** (Welle O, 2026-09-04): sie LIEGT quer in jeder Verkaufs-Auslage, in der Wette und im Wurf (um die Blickachse gerollt, Langseite waagerecht), und sie STEHT aufrecht nur noch im MAGAZIN — in der WERKSTATT liegt sie seit der Welle W (2026-09-05) FLACH, Netz nach oben, denn eine stehende Karte zeigt an der 15°-Station nur ihre Kante; seit der WELLE X liegt sie so in ihrer ETAGE im TURM, die KONTAKTE nach Bild-RECHTS (das ist Welt +Z, und die liegende Karte tut es schon von selbst — ihr Fuß zeigt dorthin). **STEHEND ist sie seit der Welle P HOCHKANT** (`DataCellView.STAND_YAW`, eine Vierteldrehung um die Hochachse, die wie der Roll der LAGE folgt): ihre Fläche zeigt nach Bild-LINKS, von oben ist sie ein SCHMALER, TIEFER Balken (gemessen 35,9 × 88,5 px statt 88,5 × 35,9), und im MAGAZIN stehen die Karten damit Fläche an Fläche wie eine KARTEI. Die ×n-Marke schaut MIT der Fläche (Welle R) — sie ist ein Kind des Körpers und dreht mit ihm. LIEGEND ändert sich nichts. Die EINE, hochkante Netz-Backung bleibt und füllt sie ohne Rand — auf der stehenden Karte liest sie darum um eine Vierteldrehung gekippt, und dafür gibt es das SUMMEN-NETZ im Streifen. `DataCellView.STAND_HEIGHT` (= `HEIGHT`) ist ihr aufrechtes Maß, an dem JEDE senkrechte Rechnung mißt — Einsinken, Aufstieg, Griff-Hub und die Tiefe jeder Grube; das ortsabhängige `stand_measure` ist tot. Wer eine Fassung baut, paßt SIE der Größe an — der TURM rechnet seinen Fußabdruck aus der liegenden Kartengröße plus Luft (`WorkshopView.TOWER_ROOM`, darin steckt `TowerView.BAR_SHARE` für die Kontaktleiste) und läßt notfalls den ganzen Werkstatt-Streifen nach rechts wachsen, nie umgekehrt. **Die Karte ist aus GETÖNTEM GLAS, sie steckt (im MAGAZIN) nur zu EINEM VIERTEL, und der GRIFF zieht sie zu dreien heraus** (Spieler-Entscheide 2026-09-04, Welle Q): Gehäuse und Kern sind alphagemischt in der Sortenfarbe (`GLASS_BODY_ALPHA` 0,32, `CORE_ALPHA` 0,45), das Netz-Quad beidseitig — von HINTEN liest dieselbe eine Backung durch den Körper hindurch, gespiegelt, und Kopfkante, Blende, Kragen, Finnen und Siegelband bleiben MASSIV; `SUNK_SHOW` steht auf 0,75 (drei Viertel stehen über dem Blech), `PIT_HOVER_LIFT` auf 0,78 (drei Viertel über der Grubenkante). **Die KAPPE ist am 2026-09-04 gestorben (Welle R): die SORTE sagt die FARBE, die GRÖSSE ihre INTENSITÄT** — `tier_tint()` entsättigt die Sortenfarbe stufenweise zum Grau GLEICHER Helligkeit (`TIER_SATURATION` 0,55 / 0,8 / 1,0), `tier_energy()` dämpft jede Emission (`TIER_ENERGY` 0,70 / 0,85 / 1,0 — der Faktor dämpft nur, denn die authored Energien stehen schon an der Bloom-Schwelle 0,95); beide lesen JEDE getönte Fläche (Glas-Körper, Kern, Kopfkanten-Saum, Rahmen, Netz-Backung). Die Rahmenstärke (`TIER_LIP_GAIN`) bleibt der stille zweite Kanal; Sortenzeichen, Größen-Streifen, goldener Kolossal-Kragen und der Glyphen-Ofen sind mit der Kappe gestorben, `CAP_DEPTH` überlebt als reines Griffmaß `GRIP_DEPTH` (dieselbe Zahl — Teilung von Reihe und Magazin bleiben byteweise gleich), und die ×n-Marke liegt in JEDER Lage auf der KARTENFLÄCHE (freie obere Ecke des Netz-Kreuzes). Siehe *Die Data-Cells* in `scripts/table/CLAUDE.md`.
- **Buchung vor dem Licht**: `GameRun` bucht, die Zeremonie zeigt nur an — neue Quellen buchen VOR dem Flug; die wenigen benannten Zeremonien, die bewusst am Einschlag buchen (Bank-Discharge, Frankiermaschine, Geld-Pakete), sind phase-/run-guarded. Nie doppelt.
- **Endzustand zuerst** (`seat_hard`-Regel): jede Fahrt schreibt erst den fertigen Zustand und fährt dann den Weg dorthin; ein abgebrochener Tween darf nichts schulden.
- **EIN idempotenter Schreiber je Körper-Ort**, mit Generations- und Lauf-Marken; jeder Abbruch läuft durch EINEN Aufräum-Pfad.
- **ui/ fasst nie Körper an**: Fenster malen Fassungen und MELDEN Rects/Anker in Display-Pixeln (das `apron_bottom`-Muster); die Körper gehören `scene_root`.
- **Ein Würfel wird nie zweimal gezeigt.**
- **Der VORRAT hat ZWEI ANSICHTEN, und der RASTER-UMSCHALTER legt um** (2026-09-03): KÖRPER (die schwebenden Würfel) oder RASTER (die GLAS-ANSICHT — der Vorrat versinkt, der Gruben-Schirm fährt zu und trägt die Anzeige, und darauf liegt das Netz-Raster des Vorrats, Sitz i = Zelle i). Der Umschalter ist eine flache Taste auf dem freien Filz ÜBER der Grube (`RasterSwitchView`, bündig mit deren Bildschirm-rechtem Ende — die eine Kante, an der neben dem Loch noch Platz ist, und sie steht an Pool- wie Werkstatt-Station im Bild); ihre Aufschrift nennt, was der Druck LIEFERT („RASTER" / „KÖRPER"), und blind bleibt sie STEHEN und wird nur grau. **Der ZELL-TIPP bedeutet zweierlei, und der Neuzugang entscheidet**: wartet einer im Ausgabefach, ist die Zelle sein Tausch-Ziel (`exchange_pending_die`, gebucht sofort), wartet keiner, wählt sie wie der Tipp auf den Pool-Körper das ZIEL der Serie (`WorkshopView.set_target_die`; die Bühnen-Fahrt aufs Podest wartet auf den stehenden Vorrat, holt ihn also beim Schließen) — und die gewählte Zelle trägt den Gold-Saum. Zug legt um, Rechtsklick schließt, das ZURREN schließt HART (das Raster darf nur stehen, solange der Vorrat stehen dürfte). **ZWEI Wege hinein, EIN Weg hinaus** (`_close_deck_glass`, hart `_settle_deck_glass_hard`): der UMSCHALTER öffnet den DAUER-Modus (`_deck_glass_pinned`) — er rahmt NICHT um, merkt sich keine Station und überlebt darum den Stationswechsel; der TIPP auf einen Neuzugang öffnet die enge Tausch-Frage, die ENG heranfährt (Rahmen = Pool-Loch ∪ Neuzugangs-Säule, ~85 % Bildfüllung, TEMPORÄRER Rahmen ohne Moduswechsel), an die gemerkte Station zurückkehrt und beim Wegzoomen abbricht. **Liegen mehrere Neuzugänge, bleibt sie nach einer Wahl STEHEN** (die SERIE, 2026-09-01): der nächste rückt ins Fach nach, Kopfzeile und Info-Säule wechseln auf ihn — und weil `exchange_pending_die` die Indizes verrutschen läßt, ist der offene Neuzugang IMMER Index 0. Das Raster prüft seine Frische am INHALT (`DeckGlassView.pool_signature`), nicht an den Referenzen: der Tausch schreibt per `become` in die Instanz, die Liste bleibt gleich. Die Pool-Maschine kennt für das Raster eine ZWEITE Park-Konfiguration (tief, mit Schirm); die der RUNDE (flach, schirmlos) bleibt unberührt. Das Raster wohnt seit 2026-08-31 in einem EIGENEN, nicht-HDR-SubViewport mit `transparent_bg`: nur so trägt es echtes Per-Pixel-Alpha (der Helligkeits-Key und die Filz-Schwärzung sind damit gestorben), und seine Auflösung wird an der GEMESSENEN Bildschirm-Pixeldichte des Schirms bemessen statt am fernen Ausschnitt der einen Anzeige — gemessen 1,35 Texel/Pixel an der Pool-Station und 1,64 an der Werkstatt-Weitsicht (dort ist das Raster nur KLEINER: Netz-Zelle ~7,8 Bildschirm-px gegen ~11,7 am Pool, gemessen bei 1280 × 720). Siehe *Die GLAS-ANSICHT* in `scripts/ui/CLAUDE.md`.
- **Es gibt EIN PODEST, und es steht als ERSTE SPALTE der Werkstatt-Zeile** (Welle X, 2026-09-05; in der Welle W stand es am Fuß der Treppe, davor mittig auf der Zeilenhöhe der Schacht-Reihe, Welle S — das ERGEBNIS-PODEST samt seiner ganzen rechten Spalte ist gefallen, mit ihm die ÜBERGABE, `_bench_side`, `result_stage`, `_result_stage_target`, `_settle_bench_handover_hard` und das Signal `die_handover`; davor war schon der FACH-SITZ als Podest gestorben). Es liegt ganz LINKS im Streifen, vor dem ZIEL-NETZ (`WorkshopView.target_net_center()`/`target_projector_y()` → `scene_root._bench_podium_target`), schwebt auf `CLAMP_HOVER`, und sein leerer Puck ist der EINE `_seat_podium_puck` — der auch WÄHREND des Durchlichts steht, denn dann fliegt der Würfel über dem Turm. Seine SPANNE ist ein WELTMASS (`stage_px()` = Würfelfläche × `STAGE_FACES` 2,4; die u-Zahl `STAGE_HEIGHT` ist nur noch der kopflose Rückfall). Der PLATZ ist die MITTE der ZEILE plus `WorkshopView.BENCH_TILT_TRIM` (18,8 Display-px, GEMESSEN): ein schwebender Körper projiziert an der geneigten Station nach oben, also rückt sein Glas-Anker um genau diesen Betrag tiefer — gemessen liest der Würfel danach 3,1 Display-px unter der Zeilenmitte (Weitsicht) bzw. 6,8 px (Nahsicht, 0°). Der TIPP auf einen VORRATS-Würfel wählt ihn als ZIEL der Serie (`WorkshopView.set_target_die` — derselbe wählt ab, ein anderer wechselt), und er fährt per **Bühnen-Fahrt** aus dem Pool auf das Podest; sein Pool-Sitz bleibt derweil LEER (`_bench_die` → `DiceTrayView.seat_shows`, die eine Sichtbarkeitsregel), und seine AUSKUNFT trägt das EINE NETZ darunter. Das gilt an JEDER Station: `_wanted_bench_die()` fragt nur noch das Fenster, nicht mehr die Kamera — die Fahrt selbst wartet ohnehin auf den stehenden Vorrat. Rechtsklick wählt ab; ein Kamerawechsel NICHT (das Podest gehört dem Fenster, nicht der Station). Die SCHABLONE (Welle U), der stehende BLOCK (Welle V) und die LAWINE samt Treppe und Scanner (Welle W) sind alle am 2026-09-05 gestorben: jetzt läuft DAS DURCHLICHT. Alle Karten rasten nach rechts in ihre Kontaktzungen ein, der Zielwürfel fliegt vom Podest ÜBER den TURM, und aus der KAMMER am Turmfuß steigt ein LICHT durch alle Etagen — jede Karte, die es passiert, flammt auf, gibt ihre Zellen an das mitreitende LICHT-NETZ ab und BRENNT AUS. Oben zerfällt das Netz in bis zu sechs LICHTFUNKEN, die ALLE ZUGLEICH in die Seitenmitten des Würfels schlagen; dort steht er mit seinem gebuchten Stand, und dann fliegt er zurück — siehe *DAS DURCHLICHT* in `scripts/ui/CLAUDE.md`. Den Ort schreibt allein `_seat_bench_place(die)`; der EINE harte Aufräum-Pfad ist `_migrate_bench_to_pool`, und `_reset_bench_migration_hard` setzt die Fahrt IMMER hart (die Bühne landet, jeder Pool-Sitz gibt seine Fahrt-Marke zurück) — ein Loch gräbt sie seit der Welle T nicht mehr.
- **Der DOPPELKLICK auf den Podest-Würfel öffnet die INSPEKTION** (2026-09-01, an der Straße seit 2026-09-03 die einzige Nah-Geste): die Kamera fährt SUPER NAH heran — Rahmen = halbe Raumdiagonale mal `CameraRig.DIE_FOCUS_MARGIN`, gemessen ~7× näher als die Station —, der Würfel legt sich in die Dreiviertel-Ansicht (`die_focus_basis`, drei Seiten auf einmal), die Kamera steht STILL, und ein ZUG dreht IHN um die Bild-Achsen (`FloatingDie.spin`; ohne Fokus dreht derselbe Zug ihn an Ort). Es gibt **keinen Seiten-Pick** — hier wird gedreht und geschaut, nicht graviert. Sie ist **MODAL wie die Glas-Ansicht**: im Bild steht nur der Würfel, `_screen_forwards_pixel` schweigt, und WASD wie Doppelklick-Nahsicht sind taub. Wie die Glas-Ansicht ist es ein **TEMPORÄRER Rahmen**: `zoom_die_focus` meldet keinen Wechsel und rührt den Modus nicht an; die Lage davor merkt sich `scene_root._die_focus_home`. Rechtsklick und Rad-zurück gehen **EINE Stufe** aufs Podest zurück (`_leave_die_focus`), erst der nächste wählt ab — Fokus → Podest → Abwahl.
- **Das Bewegungs-Gesetz liest sich nach URHEBER**: was der *Spieler zahlt*, fliegt als KÖRPER im ballistischen Bogen (der WURF, `scene_root`s `#region Der WURF`); was der *Tisch liefert*, reist als LICHT über die gelegten Adern (die AUSNAHME seit der Korrektur-Welle I: Kassetten-Lieferungen ANS MAGAZIN fliegen als reiner METEOR-BOGEN ohne Ader, denn die Hub→Magazin-Ader ist gefallen; die Quell-Adern von Automat und Schwarzmarkt leben weiter); was er *präsentiert oder einzieht*, fährt per HEBEBÜHNE durch die Fläche. Kürzer gesagt (Welle O): **was der SPIELER bewegt, fliegt ÜBER dem Tisch; was der TISCH bewegt, fährt UNTER ihm** — MAGAZIN ⇄ SCHACHT ist ein Spieler-Zug und reist darum als TRAGE-BOGEN (`DataCellView.arc_to`, `scene_root._carry_data_cell`/`_return_data_cell`): sauber, ohne Trudeln, niedriger Bogen, kurzes Setzen. Seit der Welle T (2026-09-05) fliegt auch der ZIELWÜRFEL so: Pool ⇄ Podest ist eine Zielwahl, also ein Spieler-Zug (`FloatingDie.carry_to`, `scene_root._ride_bench_from_pool`/`_ride_bench_to_pool`) — die zwei Hebebühnen-Beine samt ihrem Loch sind gestorben. **Das ABSINKEN der verbrauchten Serien-Karten ist mit der Welle U (2026-09-05) GESTORBEN** — sie werden vom LICHT gelesen und brennen an Ort und Stelle aus. Der Würfel dagegen fliegt: PODEST ⇄ TURMKOPF ist eine Zielwahl in Bewegung, also ein Spieler-Zug und derselbe `FloatingDie.carry_to`-Bogen wie Pool ⇄ Podest (`scene_root._ride_die_over_tower`). **Das DURCHLICHT selbst ist LICHT und darum keine Ausnahme**: es steigt, es fliegt nicht. Unter dem Tisch bleiben die Lieferung ins Magazin und die Fahrten, die der TISCH führt (Auslagen, Schluck, Wett-Tresen). Umgesetzt ist der Wurf bisher für den Nebenwetten-EINSATZ — Laden und Werkstatt zahlen noch Licht, ihr Umzug ist eine spätere Welle. Die ABLAGE eines benutzten Würfels ist dagegen ein EINZUG des Tisches und fährt darum per HEBEBÜHNE: eine würfelgroße Sektion öffnet unter seinem Liegeplatz in der Wurfgrube und senkt ihn ein (der SCHLUCK, `scene_root._swallow_dice`; der ballistische Ablage-Flug der Kuhle ist 2026-08-30 gestorben, siehe *Die WÜRFEL-BÜHNE* in `scripts/table/CLAUDE.md`). **Die eine benannte AUSNAHME ist die Wett-STEUER** (`scene_root._fly_bet_tax`): die Kleinsteuer je Hand ist kein Zahl-Moment, sondern ein Ticken, und sie reist darum als Meteor vom Schatz zum Wettfenster, wo der Bedingungs-Text kurz aufleuchtet. **Die zweite benannte AUSNAHME ist die GRUBE** (Welle Y, 2026-09-05): *innerhalb EINER Grube reist ein Spieler-Zug DURCH die Grube, nie über ihre Kante* — genau dafür sind Magazin-Grube und Turm-Bucht verbunden. MAGAZIN ⇄ ETAGE bleibt ein Trage-Bogen, aber er spielt unter dem Tisch: die Karte legt sich in ihrem Loch um, gleitet durch den DURCHBRUCH und steigt in der Bucht auf ihre Etage; sein Scheitel ist gedeckelt (`scene_root._pit_carry_peak`, Luft `PIT_CARRY_CLEAR` unter der Kante), gemessen bleibt der höchste Punkt jeder Karte bei −0,11 Welt-Einheiten. Neue Ausnahmen werden hier benannt oder es gibt sie nicht.
- **Die WERKSTATT ist eine SERIENSCHALTUNG** (2026-09-01): jede Gravur-Kassette trägt ein bei der Erzeugung gewürfeltes, überall SICHTBARES Prägenetz (`Pack.stamp_net`); SECHS ETAGEN stehen ab Runde 1 (die Reihenfolge ist die HÖHE: das Licht steigt von unten, was HÖHER liegt wirkt später — auf die größere Summe — und liegt dem Würfel näher) (Spieler-Entscheid 2026-09-04: die Hub-Leiter `SERIES_LADDER` ist auf EINE Sprosse geschrumpft), und seit der Welle U (2026-09-05) ist `SERIES_SLOT_CAP` **HART 6** — der Deckel IST die Blockgröße, also heben Taktgeber, Wett-Schub und Kettentreiber ihn nicht mehr (ihre Wirkung ist damit TOT; was sie statt dessen tun sollen, ist eine OFFENE Spieler-Entscheidung); bis zu `series_slots()` Karten stecken in Reihenfolge, `SeriesResolver` summiert sie seitenweise (Operatoren wirken auf die aufgelaufene Summe — die REIHENFOLGE ist die Entscheidung), und der GRIFF projiziert alles auf EINEN spielergewählten Würfel, der per Trage-Bogen aus dem Pool kommt. Seit der Welle V (2026-09-05) ist der Griff UNBEGRENZT (`press_uses`/`press_allowed`/`reset_press_cycle` sind tot, die ERDUNGSKLEMME damit wirkungslos — OFFEN) und verlangt dafür eine VOLLE Reihe: SECHS Karten, denn der Block IST sechs. Unter der Klausel KURZSCHLUSS (`SHORT_CIRCUIT_SLOTS` 1) ist die Werkstatt damit unbenutzbar — der Griff sagt das ehrlich, umgebaut ist sie nicht (OFFEN). Presse-RNG, Multicast, Ablage, Platzier-Zweiklick und das Aufspannungs-Los sind tot; der Zufall wohnt im Laden-Angebot. Siehe *Die SERIENSCHALTUNG* in `scripts/ui/CLAUDE.md`.
- **Die WERKSTATT ist ein TURM in EINER breiten, flachen ZEILE UNTER dem Pool** (Welle X, 2026-09-05; die TREPPE der Welle W ist damit gefallen, mit ihr `StairView`, das FACH, die NASE und die ganze senkrechte Spaltenrechnung): der Streifen hat **keinen Schirm** (`StyleBoxEmpty`, keine `window_*`-Uniforms im Glas) — seine Teile liegen auf dem Filz. Er liest seit der **WELLE Z** (2026-09-05) von LINKS nach RECHTS als EINE Zeile: **SUMMEN-NETZ samt CAPTION** | **ETAGEN-LEISTE** | **AUSWURF-BAHN** | **TURM** | **ZIEL-NETZ** | **PODEST** (der Zielwürfel) | **GRIFF**, darunter das MAGAZIN über die volle Breite — die GRUBE liegt damit in der linken Hälfte, der WÜRFEL in der rechten. Die BUCHT steht seither UNTER der Pool-Reihe (die gemeldete Mindest-Kante `min_tower_left` ist gestorben): der Turm ragt nirgends heraus, und der Würfel über ihm projiziert 14,5 px nach oben gegen eine Naht von 33,7 px. Am rechten Ende hält dafür eine ZWEITE gemeldete LEHNE (`die_lean`, 46,9 px) den GRIFF-Knopf vom Podest-Würfel frei. **DER TURM ist ein gerades Gestell aus sechs ETAGEN** (`TowerView`, ein Körper): Etage 1 UNTEN, jede weitere um `FLOOR_PITCH` höher (= eine KARTENDICKE `DataCellView.DEPTH × CASSETTE_SCALE` = 0,330 plus `FLOOR_PLATE` 0,06 = **0,390 Welt**), auf einer KAMMER (`CHAMBER_HEIGHT`) als Sockel; sechs Etagen sind **2,554 Welt** hoch. **Und er steht IN einer GRUBE** (Welle Y, 2026-09-05): kein Upgrade ragt mehr über den Tisch. Die Magazin-Grube und die neue TURM-BUCHT sind EIN L-förmiger Raum aus zwei Rechtecken — gleiche Tiefe (`scene_root._pack_pit_depth()`, **4,600**), EIN durchgehender Boden (Welle Z: das MAGAZIN baut die einzige Platte über die ganze L-Fläche, die BUCHT baut keine — `floor_area`/`build_floor`, `floor_sink` ist tot), und an der gemeinsamen Kante steht KEINE Wand: die Bucht ist zum Magazin hin ganz offen, die Magazin-Wand hat dorthin genau EINEN Durchbruch (`PackPitView.setup(..., open_wall, breach)`, Löcher-Platz `TableScreen.PIT_TOWER`). Der Turm sitzt auf dem Grubenboden (`_pit_floor_drop()` 4,630), sein Kopf liegt damit **2,07** Welt-Einheiten UNTER der Tischkante und die oberste Karte 2,40; gemessen erreicht KEINE Karte je mehr als **−0,11**. Die Bucht deckt Turm UND Auswurf-Bahn (`WorkshopView.tower_pit_rect()`, gemessen **333,0 × 167,4 Display-px**), die ETAGEN-LEISTE bleibt links davon auf dem Glas — im Loch wird kein Display gezeichnet. Der Würfel darf über die Kante ragen, er ist kein Upgrade: er schwebt für das Durchlicht auf `TOWER_DIE_CLEAR` über dem Turmkopf, also 0,33 über dem Tisch. Weil der Turm nicht mehr aus der Fläche projiziert, mißt `_tower_head_px()` nur noch DIESEN Würfel (**14,5 px** statt 87,0), und die Kamera kommt auf **11,85 statt 13,5** Welt-Einheiten heran. Ein Etagenboden ist kein Blech, sondern ZWEI SCHIENEN an den Längskanten (`RAIL_SHARE` 0,13) — nur so sieht man von oben durch den Turm und das DURCHLICHT in ihm. Rechts steht die goldene KONTAKTLEISTE mit einem MUND je Etage, IM Fußabdruck (`BAR_SHARE` 0,07); links ist der Turm OFFEN. Die Karte LIEGT flach in ihrer Etage, Kontakte nach Bild-rechts, und NICHT eingerastet um `UNLATCHED_PULL` (0,25 ihrer Länge) plus `PEEK_SHARE` (0,023 je Etage, unten am weitesten heraus) nach LINKS gezogen — von oben liest darum die OBERSTE Karte ganz, die darunter als Farbstreifen. **Die ETAGEN-LEISTE ist das 2D-Gegenstück** und trägt Klick, Zug und Zeiger (der Turm selbst trägt keinen Klick): sechs Felder, unten „1“, leer dunkel mit Nummer, belegt in der Sortenfarbe (Intensität = Größe, dieselbe `TIER_SATURATION`-Leiter wie die Karte), Operator amber. HOVER zieht die Karte über den ZWEITEN Hover-Kanal der Zelle (`DataCellView.hover_slide`, `TowerView.HOVER_SLIDE` 0,75) längs ganz aus dem Turm in die AUSWURF-BAHN — freier Filz von einer Kartenlänge, damit ihr Netz frei liest, ohne Leiste oder Netz zu verdecken. **Gemessen** (1280 × 720, Anzeige 4680 × 3180, u = **3,559 px**): Fenster **977,0 × 155,9**, `bench_rect` 977,0 × 268,7, Zeile 959,9 × 145,9, Podest 94,0 × 94,0, Ziel-Netz 174,8 × 131,7, Summen-Schirm 174,8 × 145,9, Leiste 39,2 × 145,9 (Feld 39,2 × 22,8), Auswurf-Bahn 147,9, Turm **156,9 × 97,3**, Griff 53,4 × 14,2, Magazin 977 × 105,3. Der Streifen braucht damit nur 268,7 der 802,0 px freien Filzes unter dem Pool — die Kamera kommt dafür auf **13,5 statt 21,0** Welt-Einheiten heran (36 % näher als die Treppe). **Eine NETZ-ZELLE IST die FLÄCHE eines echten Würfels** (Welle S): `scene_root._die_face_px()` projiziert `DieBuilder.FACE_SIZE × DiceTrayView.DIE_SCALE` in Display-Pixel (gemessen **39,15 px**, Kante 45,0) und meldet sie als `WorkshopView.die_face_px`; BEIDE Netze teilen sie, ein Netz mißt damit 168,3 × 125,3 px. Beide stehen auf BLANKEM FILZ und **IMMER da**: ohne Inhalt als leeres Kreuz, nie versteckt. Der SUMMEN-Schirm trägt die REINE Summe aller gelegten Prägenetze (zielunabhängig, `PressNetView.sum_net`) — beim Hover über eine Karte (Etage ODER Magazin) statt dessen DEREN Prägenetz —, darunter EINE Zeile (die CAPTION, `set_caption`): Zell-Klartext schlägt Kartenname schlägt den GRUND, aus dem der Griff schweigt (`grip_blocker()`). Das ZIEL-Netz zeigt die Vorschau und nach dem Griff das Ergebnis mit grünen Deltas. **`bench_rect()` streckt sich MIT — nach UNTEN bis zur ECHTEN Magazin-Unterkante**: genau so ein Überhang war in der Welle H der Bug „Upgrades lassen sich nicht wählen“; ein Test schickt darum einen Chip-Tap im unteren Grubenteil durch den ECHTEN Forward-Pfad. Das Fenster MALT den Turm NICHT: es malt die Leiste und MELDET Rechtecke und Anker (`tower_rect`/`strip_rect`/`floor_field_rect`, `press_slot_anchors`/`press_display_anchors`, `target_net_center`/`target_projector_y`, `ist_net_center`) — die Körper gehören `scene_root`. **Die HÖHE ist GEGEBEN statt gelöst**: sie ist die HÖCHSTE der drei Spalten (Summen-Schirm, Turm-Fußabdruck, Bühne) plus die beiden Ränder, und **u folgt der WÜRFELFLÄCHE** (`unit_for(die_face)` = `die_face / U_PER_FACE` 11,0) statt einem Höhen-Budget — im Streifen ist fast alles ein Weltmaß. `scene_root._fit_workshop_rect` legt ihn an: linke Kante bündig mit der POOL-REIHE, Oberkante eine Naht (`_pool_seam_px`) unter ihr, und warnt, wenn er nicht mehr in den freien Filz paßt. Die BREITE zählt jede Spalte und fünf Fugen (`bench_width_for`), **plus zwei GEMELDETE Zulagen**: `min_tower_left` (der Turm muß rechts der POOL-REIHE stehen, denn über ihm liegt sein Kopf samt Würfel im Bild) und `tower_lean` (ein hoher Körper wächst vom Bildmittelpunkt weg — ohne die Zulage deckte die oberste Karte den GRIFF zu). Die Kamera (`configure_workshop_target`) rahmt Streifen und Magazin plus den KOPFRAUM von Turmkopf und Würfel darüber (`_tower_head_px()` = ihre Welthöhe mal dem gemessenen Neigungs-Versatz `BENCH_TILT_TRIM/CLAMP_HOVER`, **87,0 px**) und links den des schwebenden Podest-Würfels (`_hover_die_head_px()`); dieselben Zulagen bekommt die Nahsicht, dort plus den Kopfraum der stehenden Magazin-Karten. `CameraRig.tilted_fit_distance` mißt seit dieser Welle auch die BREITE an den vier Ecken (im geneigten Blick bildet sich die NAHE Kante größer ab — das schlug erst zu, als der Streifen flach und breit wurde), und `WORKSHOP_MIN_DISTANCE` (7,0) ist nur noch eine Sicherung statt des alten `ZOOM_DISTANCE`-Bodens. Siehe *DER TURM* in `scripts/ui/CLAUDE.md`.
- **Die LADUNG** (2026-09-05): jeder Würfel trägt `DieDefinition.charge` (0–`CHARGE_MAX` 3, Stufen Glimmen/Kriechstrom/Überschlag) und `burned_out`; beide reisen in `become` UND `instantiate`. **Grundregel: +1 Mult je Ladung** aller gewerteten Würfel der Hand (END-Stand, durchgebrannte zählen 0) — ein eigener Schritt VOR den statischen Charms. **Jede ZÜNDUNG der oberen Seite würfelt** (`DiceScoring.CHARGE_CHANCE` 0,5) auf +1; Pointer-, Essenz- und Runen-Glieder würfeln nicht, Retrigger würfeln jeder für sich (0 → 3 in EINER Hand ist möglich). **Das Urteil fällt ERST, wenn der Würfel FERTIG gefeuert hat**, vor dem nächsten Würfel (Spieler-Entscheid 2026-09-06): alle seine Zündungen zählen, dann steigt er um die Treffer. **Durchbrennen** trifft nur, wer mit der Spitze in die Hand GING — ebenfalls erst nach seiner letzten Zündung. Ein durchgebrannter Würfel zählt für die HAND-ERKENNUNG, liefert aber 0 Augen und feuert NICHTS — spiegelgleich in `DiceScoring._base_and_mult`, `ScoreBreakdown.build` und `MaterialEffects.apply_take_effects`. **Rundenende**: jeder Vorrats-Würfel, der in dieser Runde nicht gewertet hat, verliert eine Stufe. **Erzwungenes +1** (Fumble, Griff, Klausel) würfelt nie und brennt an der Spitze sicher durch (`GameRun.charge_up_forced`). **Bucht-Methoden** `repair_die` (3 ⚡, Isolierband $15), `drain_die` ($5) und `charge_die` (1 ⚡, bis zur Spitze) sind check-then-spend und verweigern bei `repair_locked()`; „Alle entladen“ ist tot. **Buchung vor dem Licht**: `GameRun.book_charge_results` bucht beim Nehmen VOR der Zeremonie; die Zeremonien der Welle 3 hängen an den Rückgabewerten. **Am KÖRPER** trägt jeder Würfel die vier Zustände über den EINEN Refresh-Pfad (`DieFaceDisplay.apply_definition` liest `charge`/`burned_out`): Glimmen als Hitze, die aus dem Würfel wabert, Kriechstrom als Blitze, die über die Seiten springen (die Kante bleibt ungefärbt), Überschlag zusätzlich als Eck-Lampen und Blitze, die aus den Kanten nach außen schlagen, Ruß als flache dunkle Kante ohne Lache — die Farbe ist NIE das Energie-Cyan (siehe *Die LADUNG am Körper* in `scripts/dice/CLAUDE.md`). **Die LADESÄULE** (`ChargingColumnView`, `CameraRig.Mode.REPAIR`) steht als eigene Station RECHTS neben dem PODEST der Werkstatt-Zeile und ist ein KÖRPER statt drei Knöpfen (Spieler-Entscheid 2026-09-07, `RepairBayView` ist gestorben): drei ELKO-DOSEN der Kondensatorbank zeigen die Ladung des Zielwürfels, zwei KIPPHEBEL laden auf und leiten ab, ein SICHERUNGSSOCKEL am Fuß repariert, je Bedienelement ein Preisschild, und ein KABEL läuft zum Podest-Puck. Sie LEHNT sich dem Blick entgegen — senkrecht gestapelt fielen die Dosen an der 15°-Station in EINEN Fleck. Sie bucht nichts, sondern MELDET; die Bremsen kommen aus der reinen Regel `RepairRules` (siehe *Die LADESÄULE* in `scripts/table/CLAUDE.md`). Volltext: *Die LADUNG* in `scripts/core/CLAUDE.md`.
- **UI-Wort vs. Code-Wort**: Energie/`energy`, Ladung/`charge`, Gravur/`Engraving`, Veredelung/`dope`, Pointer (die LED-Adern behalten „Leiterbahn"), Benchmark (nie „Blind"/„Ante").
- **Sichtbar heißt bedienbar**: was bedient werden darf, hängt an Sichtbarkeit, nie an der Kamera-Station. Es gibt genau ZWEI benannte Ausnahmen: der Setzen-Klick des Wett-Tresens (2026-08-26) — gewettet wird nur an der eigenen Station, aus der Ferne wird der Klick zum Zoom — und der Schwarzmarkt-KAUF (2026-08-30): gekauft wird nur an der eigenen Station, aus der Ferne wird der Klick zum Zoom; Hover, Schilder und Tooltip antworten überall, wo die Ware steht. Neue Ausnahmen werden hier benannt oder es gibt sie nicht.

## Arbeitsweise (Spieler-Entscheidung 2026-08-25)
- **Delegiert wird nur bei echten FEATURE-WELLEN** (neue Mechanik, mehrere Subsysteme, Umbau einer Grammatik) — dann Plan-Datei + Opus-5-Subagent wie gehabt. **Kleine Änderungen macht die Session SELBST**: Ein-/Zwei-Datei-Fixes, Konstanten, Farben/Maße, visuelle Tweaks, Bugs mit bekannter Wurzel. Kein Plan-Dokument, kein Agenten-Aufsatz für so etwas.
- **Getestet wird EINMAL AM ENDE**, nicht nach jedem Schritt: ein Suiten-Pass, wenn die Änderung steht (plus Boot, falls `scene_root.gd` angefasst wurde). Kein Wiederholen der Läufe, die ein Subagent gerade grün gefahren hat — sein Log prüfen genügt (immer mit dem `Ignoring|Parse Error`-Grep, siehe unten); der eigene Vollpass gehört vor den Commit. **Das gilt auch INNERHALB einer Agenten-Welle**: der Agent arbeitet mit `-gselect` auf dem einen Skript, das er gerade anfaßt, und fährt die Suite EINMAL ganz am Schluß — jeder Agenten-Brief sagt das ausdrücklich, samt `--quit-after`, Vordergrund-`timeout` und dem Verbot von `run_in_background` für Godot (siehe *Commands*).
- **Beweis-Aufwand nach Anlass**: Kennfarben-Aufnahmen, Pixel-A/B und Frame-Serien gehören zur Artefakt-Jagd (dort haben sie dreimal die echte Ursache statt einer Vermutung geliefert) — nicht zu Geschmacks-Änderungen.

## Testing
Notizen und GUT-Eigenheiten: `test/CLAUDE.md`. Tests parsen `scene_root.gd` nie — nach Änderungen daran das Spiel einmal headless booten.
